struct A { A a }

fib(int n) -> int {
    if(n <= 1) {
        return n;
    }
    else {
        return fib(n-1) + fib(n-2);
    }
    return 0;
}

main() {
    int n = 10;
    int result = fib(n);
    print(result);
}