union A {
    int,
    float
}
main() {
    A a = 1;
    match(a) {
        case (int a) {
            print("Int: " + a);
        }
        case (float a) {
            print("Float: " + a);
        }
    }
}